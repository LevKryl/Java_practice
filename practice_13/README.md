# Статические методы

## Все варианты вызова статического метода printVars()

1. Через имя класса
   A.printVars();

2. Через объект класса
   A obj = new A();
   obj.printVars();

3. Через переменную (даже null)
   A ref = null;
   ref.printVars();



